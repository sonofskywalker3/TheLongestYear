# TLY 0.13.0 — Sub-project A: Owned-Bundle Engine — Design (WIP / mid-brainstorm)

**Status: SUPERSEDED 2026-07-14** — the engine was pulled forward into 0.12.0 by user
direction; the finished spec is `2026-07-14-tly-0.12.0-owned-bundle-engine-design.md`. This doc
is kept for decision history only. Notable revisions made there: seasonal foraging bundles stay
seasonal (no any-season pool), Gemstone → Mineral bundle (gems reserved for the Hard-tier Jewel
bundle), Vault engine-owned at +25%, Book bundle added, surveyed roster (Tapper's, Four Seasons
Sampler, Preserver's, Home Cook's Feast, Weatherman's, Slayer's, Recycler's) adopted, and the
baseline is tuned to the NORMAL bar (not vanilla-equivalent).

**Original status:** IN BRAINSTORMING — resumed 2026-07-10: write mechanism, seeding, and migration
RESOLVED with user; remaining technical items drafted below for spec-review reaction. Next step:
fold this into a finished spec once the user reacts to the drafts (esp. new-bundle patterns).
**Date started:** 2026-06-15

## Where this sits

0.13.0 (the difficulty engine) was decomposed into 4 sub-projects, to be specced/built in order:
- **A — Owned-bundle engine (THIS DOC).** Foundation: TLY owns + per-loop re-randomizes its own bundle set, replacing the read-from-game path. Single default difficulty (vanilla-equivalent "Normal" baseline).
- **B — Difficulty tiers (Easy/Normal/Hard).** Parameterize A. Tier chosen at run creation.
- **C — Cart economy by tier.** Easy untouched; Normal = the JP-permanent cart slots already shipped in 0.12.0; Hard = gold-per-run slots + JP-lock.
- **D — Brutal Hard gate content + escape valves.** Galaxy Sword, 150 Salmonberries, artifact-any-museum, iridium roe, etc.; winnable via cart RNG / JP cultivation perks / stash carry.

(Seasonal "retaliation" escalations = 0.14.0, separate.)

Full roadmap context: user memory `tly-012-013-014-roadmap.md`. 0.12.0 (clarity + cart economy + Cart Whisperer tiers) is code-complete at manifest **v0.11.10**, local-only, unreleased.

## Current architecture (what A replaces)

- Today TLY READS the game's live bundles: `BundleCatalogBuilder.Build()` / `.BuildRequirements()` read `Game1.netWorldState.Value.BundleData`, parse via `BundleParsing`, classify rooms via `RoomThemeMap` (Pantry→Farming, CraftsRoom→Foraging, FishTank→Fishing, BoilerRoom→Mining, Bulletin/BulletinBoard→Mixed; Vault/Joja/unknown skipped). This read-and-classify path is the source of the remix-fragility bugs (#2 remix bundles missing from checkpoints, #4 impossible mining theme, blank weekly themes).
- Downstream consumers of the classified set: `BundleRequirement` (carries `Ingredients`, `NumberOfSlots`, `IngredientStacks`, `IngredientQualities`), `BundleGate.IsSatisfied`, the weekly-theme service, the season-goal checklist (`SeasonGoalsMenu`), and `CcDonationReconciler` (derives donated ids from the game's authoritative per-slot completion).
- Reset path: `RunController.FinalizeReset` → `WorldResetService.PerformReset`; `CommunityCenterUnlock.Apply()` already calls `Game1.netWorldState.Value.SetBundleData(...)` + `cc.MakeMapModifications(force:true)` and pre-reveals all 6 Junimo Notes.
- TLY is dormant on non-TLY saves (`RunActivation.IsActive`); per-save opt-in (`MetaState.IsLongestYearRun`); `MetaState.CompletedResets` = loop counter.

## DECISIONS so far (confirmed with user)

**Generation model = "expanded-pool remix":**
1. Every room **keeps its theme** (reuse `RoomThemeMap`).
2. Each room has a **bundle pool** = the existing vanilla bundles **+ new TLY-authored bundles**. Each loop, **remix which bundles appear** (vanilla-style remix, but drawing from the bigger pool incl. the new ones).
3. Each bundle keeps its **slot count**, but slots are filled from an **expanded, theme/season-appropriate item pool**, wider than vanilla, **re-randomized every loop**. Examples the user gave: *Spring Crops* can pull ANY spring-growable crop (incl. harder/year-2-ish); *Quality Crops* can ask for various vegetables, not the fixed vanilla four.
4. The "keep a little of everything across runs" meta comes from this per-loop content re-roll (you can't predict next loop's asks).

**Item pool sourcing = HYBRID:**
- **Existing bundles' expanded pools → derived from game data** at runtime (e.g. `Data/Crops` by season, fish data by location/season, forage tables). Auto-includes year-2 + modded items; needs an exclude-list for junk/quest/un-obtainable items.
- **New bundles → hand-authored item PATTERNS** (curated pools), recognized/created by us. Confirmed new bundles:
  - **Artifact bundle → Bulletin Board (Mixed theme).** Pattern: can roll museum/artifact donation items (ties to the earlier "stockpile artifact troves across runs, gamble on contents" idea).
  - **Gemstone bundle → Furnace/Vault (Mining theme or Vault room).**
  - (More new bundles may be added.)

**Difficulty hook (deferred to B, NOT in A):** Hard weights pools toward rarer/harder items and/or bumps required quantities. A ships a single vanilla-equivalent baseline.

## RESOLVED 2026-07-10 (brainstorm resumed with user)

1. **Write mechanism = (b) direct `netWorldState` write, TLY OWNS the generator.** TLY builds
   the `BundleData` strings itself from our expanded pools and calls `SetBundleData(...)` at
   run-create and on each reset. NOT vanilla's `Game1.GenerateBundles` over an edited
   `Data/RandomBundles` (its seeding is pinned to the save's uniqueID and its schema constrains
   our patterns). **Live evidence:** the 2026-07-10 verification pass ran vanilla
   `debug ShuffleBundles` mid-save — `netWorldState` rewrite worked cleanly and
   `BundleCatalogBuilder` reclassified the new set with 0 errors (`tly_classify`), proving the
   write path AND the downstream re-read.
3. **Seeding (folded into #1):** `seed = HashCode.Combine(Game1.uniqueIDForThisGame,
   MetaState.CompletedResets)`. Stable per loop — reloading or replaying a reset regenerates
   IDENTICAL bundles (no bundle-reroll save-scum, consistent with meta-commit-on-Saving).
   Generation happens ONLY at run-create and inside the reset; bundles persist in the save
   between times.
7. **Migration = regenerate on next loop.** The in-flight loop finishes on the game's existing
   bundles; the first reset after updating generates from the engine. No migration code; the
   read-from-game path is deleted, not kept alive for old saves.

## DRAFT PROPOSALS (technical — react at spec review)

2. **CC completion reset:** extend the reset's existing `CommunityCenterUnlock.Apply()` step:
   generate → `SetBundleData(newDict)` → clear stale `netWorldState.Bundles` /
   `BundleRewards` entries → `areasComplete` all false → `cc.MakeMapModifications(force:true)`
   → re-reveal the 6 Junimo notes (already there). One ordered sequence in one service.
4. **Downstream wiring stays put:** `BundleCatalogBuilder` keeps reading
   `netWorldState.BundleData` — since TLY wrote it, the whole classify → gate → weekly-theme →
   season-checkpoint chain works unchanged (proven by `tly_classify` today). Reliability win:
   the generator also emits each bundle's requirement metadata (kind, quotas, stacks,
   qualities) directly, so classification consults the generator's manifest first and the
   derived-ramp fallback becomes belt-and-braces. Remix-fragility (#2/#4/#6) dies by
   construction. `CcDonationReconciler` untouched (reads the game's per-slot completion).
8. **Stacks/qualities:** the generator assigns per-slot stack + quality from one tunable
   config block (baseline ≈ vanilla feel: mostly stack 1-5 normal quality, occasional
   silver/gold). `SeasonGoalsMenu` already renders `IngredientStacks`/`IngredientQualities`.
   Difficulty tiers (sub-project B) scale these knobs — no new surface needed there.
6. **Pool derivation (draft):** crops from `Data/Crops` by season (plantable+harvestable in
   the bundle's window); fish from `Data/Fish` + location/season spawn data; forage from
   `Data/Locations` `SpawnForageData`; minerals/artisan by `ObjectData` category. Exclude-list
   (single constant): quest/unobtainable ids, legendary fish (Hard-only later, sub-project D).
   Year-2-ish items allowed in pools at low weight. Concrete lists land in the spec.
5. **New authored bundles (draft — the creative one, react hardest here):** Artifact bundle
   (Bulletin Board, 4 slots rolled from the museum-donatable artifact pool — the "stockpile
   troves across runs, gamble on contents" meta); Gemstone bundle (Boiler Room, slots from
   gem/mineral pool). Start with 1-2 authored additions per room pool; remix keeps vanilla's
   bundles-per-room counts.

## STILL OPEN

- New-bundle rewards + exact slot counts (Q5 detail) — draft in spec, user reacts.
- Whether the engine also owns Vault money bundles or leaves them vanilla (today they're
  index-derived via `VaultBundleMap`; leaving them vanilla is the minimal draft position).

## Process note

Continue via `superpowers:brainstorming`: resolve the open questions one at a time, present the design in sections for approval, write the finished spec, then `superpowers:writing-plans`. Do NOT start implementation until the spec is approved (brainstorming HARD-GATE). Workspace rules: this is a long-lived effort but commits land on `master` (release line owns version bumps); build `dotnet build src/TheLongestYear/TheLongestYear.csproj -c Release`; tests `dotnet test tests/TheLongestYear.Tests/TheLongestYear.Tests.csproj` (493 passing). Never push/release without explicit user approval. Decompiled Android source at `C:\Users\Jeff\Documents\Projects\decompiler\stardew-valley-android`.
