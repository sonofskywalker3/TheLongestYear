# Build output
bin/
obj/

# IDE / tooling
.vs/
*.user
*.suo

# OS
Thumbs.db
.DS_Store

# Local screenshots + log captures (not part of the repo)
screenshot*.png
SMAPI-latest.txt

# Local save-file backups (user save data captured during recovery — never commit)
save-recovery-backup/

# Sprite generator zoomed previews (not shipped)
tools/preview/

# Local test/dev artifacts (logs, scrapes, recon screenshots, evidence saves) — not part of the repo
test-output/
